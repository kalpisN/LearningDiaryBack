version: 0.2

phases:
  build:
    commands: echo "Building"
  post_build:
    commands:
      - echo "Build successfull!"
      - mvn package
      - mvn target/learningdiary-0.0.1-SNAPSHOT.jar
artifacts:
  files:
    - target/.jar
    - .ebextensions/**/
    - appsepc.yml